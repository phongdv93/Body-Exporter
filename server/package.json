{
    "name": "bodyexporter-api",
    "version": "1.0.0",
    "private": true,
    "description": "Cloudflare Worker that issues short-lived JWTs for SolidWorks Body Exporter licenses.",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250101.0",
        "typescript": "^5.6.0",
        "wrangler": "^3.95.0"
    }
}
